import React from "react";
import styles from "./Calendar.module.scss";

const Calendar = () => (
  <div className={styles.Calendar}>Calendar Component</div>
);

Calendar.propTypes = {};

Calendar.defaultProps = {};

export default Calendar;
